export default {
  UPDATE_INDEX(state, index) {
    state.currentIndex = index
  }
  // ACTIVATE_PAGE(state, index) {
  //   state.pages[index].isActive = true
  //   if (index > 0) {
  //     state.pages[index - 1].isActive = false
  //   }
  // }
}
